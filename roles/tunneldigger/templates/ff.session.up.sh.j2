#!/bin/bash
# Managed by ff-berlin ansible

TUNNEL_ID="$1"
SESSION_ID="$2"
INTERFACE="$3"
MTU="$4"
ENDPOINT_IP="$5"
ENDPOINT_PORT="$6"
LOCAL_PORT="$7"
UUID="$8"
LOCAL_BROKER_PORT="$9"

# Set the interface to UP state
ip link set dev $INTERFACE up mtu $MTU
ip add add 172.31.224.1 dev $INTERFACE
